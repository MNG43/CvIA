# Fichier: src/main/resources/application.yml
spring:
    application:
        name: ai-service
    cloud:
        config:
            enabled: false
    datasource:
        url: jdbc:postgresql://postgres-vector:5432/vector_db
        username: user
        password: password
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
    kafka:
        bootstrap-servers: kafka:9092
        consumer:
            group-id: ai-group
            auto-offset-reset: earliest
            key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
            value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
        producer:
            key-serializer: org.apache.kafka.common.serialization.StringSerializer
            value-serializer: org.apache.kafka.common.serialization.StringSerializer
    ai:
        openai:
            api-key: ${OLLAMA_API_KEY:sk-test}
            base-url: ${OLLAMA_BASE_URL:http://192.168.1.167:11434}
            chat:
                options:
                    model: ${OLLAMA_MODEL:llama3.2}
                    temperature: 0.7
            embedding:
                options:
                    model: nomic-embed-text

ollama:
    base-url: http://192.168.1.167:11434
    model: nomic-embed-text

server:
    port: 8084

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/

springdoc:
    api-docs:
        path: /v3/api-docs
    swagger-ui:
        path: /swagger-ui.html
