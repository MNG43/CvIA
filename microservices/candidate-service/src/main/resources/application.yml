spring:
    application:
        name: candidate-service
    cloud:
        config:
            enabled: false
    datasource:
        url: jdbc:postgresql://localhost:5434/candidate_db
        username: user
        password: password
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
    kafka:
        bootstrap-servers: localhost:9092

    # Configuration de Feign
feign:
    client:
        config:
            default:
                connectTimeout: 5000
                readTimeout: 5000
                loggerLevel: basic
    httpclient:
        enabled: true
        max-connections: 200
        max-connections-per-route: 50

server:
    port: 8083

eureka:
    client:
        service-url:
            defaultZone: http://localhost:8761/eureka/

# Fichier: src/main/resources/application.yml (ajouter à la fin)
job:
    service:
        url: http://localhost:8081 # URL du Job Service
